# Learn List 领域词汇

本文档固定首版的产品语言和边界，避免 UI、数据库、备份和测试对同一个概念使用不同含义。

| 词汇 | 定义 | 首版边界 |
| --- | --- | --- |
| 学习项目 | 一组学习内容，可以是书籍、课程或技能。 | 可暂停、归档，拥有标签。 |
| 学习任务 | 项目中的最小复习单元。 | 不另建独立闪卡层；包含标题、回忆提示、资料/笔记和来源。 |
| 复习阶段 | 当前固定间隔在 `1/2/4/7/15/30/60/90` 天序列中的位置。 | 最高阶段为 90 天；逾期不自动改变阶段。 |
| 复习记录 | 一次真实复习的不可变记录。 | 保存反馈、旧阶段、新阶段、完成日期和下次日期。 |
| 复习纠正事件 | 对当前复习投影的人工修正。 | 追加记录，不改写旧复习记录；只从当前阶段和下次日期继续。 |
| 复习队列 | 当前日期可处理的学习任务有序集合。 | 逾期优先；建议批次可连续处理，但不隐藏未进入批次的积压。 |
| 阅读计划 | 书籍或资料的页数推进计划。 | 保存总页数、当前页、每日目标、截止日和每日阅读日志。 |
| 阅读日目标 | 某个计划在某一天应读的页数。 | 默认来自计划每日目标；均摊或手动调整后按天保存。 |
| 阅读调整事件 | 对阅读当前页数的正负修正。 | 追加带原因的 delta，不改写原始阅读日志。 |
| 必做行动 | 计入今日百分比的行动。 | 必须是到期复习/首次学习、活跃阅读计划达标或到期待办。 |
| 重复待办 | 与学习任务分开的日常事项。 | 一次性、每天、每周、工作日、自定义星期。 |
| 待办实例 | 从重复待办复制出的某一天一次性行动。 | 漏做时可复制到今天；实例不再重复询问。 |
| 专注记录 | 一段番茄钟实际产生的专注时间。 | 计入统计和目标，不作为必做行动。 |
| 番茄阶段 | 工作、短休或长休中的一个计时阶段。 | 工作 25 分钟、短休 5 分钟、四轮后长休 15 分钟；支持暂停、跳过和实际秒数。 |
| 量化目标 | 针对单一指标的周期目标。 | 专注分钟、阅读页数、复习项或待办完成；支持日/周/月/自定义截止日。 |
| 倒计时 | 指向一个日期时间的事件。 | 可完成、可备注；提醒通过系统通知。 |
| 固定提醒 | 在固定时间反复触发的项目或每日摘要提醒。 | 支持多个时间、星期、安静时段；权限不足时近似排程。 |
| 今日进度 | 当天必做行动的完成比例。 | 分母为 0 时显示“—”，不会误报 100%。 |
| 连续打卡 | 连续有至少一个必做行动完成的日期数。 | 休息日和暂停项目不打断；专注/目标/倒计时不单独贡献连续天数。 |
| 备份包 | 本机所有学习数据的可迁移文件。 | 默认推荐 AES-GCM 加密；导入前预览并选择合并或替换。 |
| 回收站 | 实体的软删除集合。 | 可恢复；永久删除会连同其附属历史一起清理。 |
| 更新传输 | Release APK 从连接、断点下载到安装器交接的状态。 | 本地记录进度，可暂停/续传；安装前校验摘要、包名、版本和签名证书。 |
| 安全快照 | 替换导入前自动生成的本地加密备份。 | 保留最近 3 份或 30 天内的快照，并保存恢复密码文件。 |

日期与时间显示统一使用应用注入的 `Clock` / `ZoneId`：表单默认日期、今日总览、专注记录、统计趋势、目标归因和更新检查时间必须使用同一时区；跨午夜时刻不能依赖设备默认时区的隐式转换。

旧版全局每日进度提醒字段只作为兼容迁移输入：首次启动或导出前转换为 `SUMMARY` 固定提醒，迁移成功后清理旧键；新代码和新备份不得把它们当作独立配置来源。详见 [ADR 0009](docs/adr/0009-unified-reminder-settings-migration.md)。

## 关键不变量

- “稍后”只调整下次提醒日期，不产生复习记录，也不改变阶段。
- 复习队列先排逾期项；批量复习只取建议批次，剩余到期任务仍可见并可单独处理。
- 复习纠正和阅读调整只能追加事件；它们不覆盖旧日志。
- 下一次复习日从实际完成日计算；“模糊”使用当前间隔一半并向上取整。
- 阅读日志不会把当前页推进超过总页数；当天目标达标才计一项必做行动。
- 暂停项目不产生今日必做项；归档项目不出现在默认学习列表，但仍保留在备份中。
- 软删除记录不参与默认列表、今日行动或提醒；恢复后重新进入对应列表。
- 番茄只有工作阶段产生专注统计；取消/跳过不计入，暂停后结束只记录实际工作秒数。
- 提醒必须在投递时再次检查实体是否启用、活跃且仍然到期，不能只信任旧闹钟。
- 一次提醒投递中的到期判断、摘要进度和通知动作必须共享同一个 `ZoneId` 与本地日期。
- 更新包必须是 HTTPS GitHub Release、版本不降级且签名证书与当前安装包一致；系统安装器仍需用户确认。
- 所有用户导入数据先通过格式/大小/密码校验，再在 Room 事务中写入。
- 每日进度提醒只能从统一 `ReminderEntity` 读取；旧版全局字段不能覆盖已有提醒或触发第二套排程。

